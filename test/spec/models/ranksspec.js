var ranks = require('../../../lib/models/ranks.js');

console.log(ranks);
console.log(ranks.length);
ranks.push('a');

console.log(ranks.length);
ranks.push('a');
console.log(ranks.length);
ranks.push('a');
console.log(ranks.length);
ranks.push('a');
